🎬 Movie Watchlist – OMDB API + LocalStorage

A simple two-page movie watchlist application built using the OMDB API. Users can search for movies, add them to a watchlist, and view their saved list — all stored in LocalStorage.